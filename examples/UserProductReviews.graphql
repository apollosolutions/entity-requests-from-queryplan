query UserProductReviews($productId: ID!) {
  product(id: $productId) {
    id
    title
    description
    reviews {
      id
      body
      user {
        id
        username
      }
    }
  }
}
